const express = require('express')
const supertest = require('supertest')

describe('Testing priceController', () => {
  it('testing priceController.getEstimate', () => {
    const res = {}
  })
  // it('testing priceController.getHistory', () => {
  //   const res = 
  // })
})